# Copia este archivo como ".env" en esta misma carpeta y rellena los valores.
# Usado tanto por zabbix.yml (zabbix-server-pgsql) como por zabbix-nginx.yml
# (web-nginx-pgsql) — deben apuntar al MISMO usuario/password de PostgreSQL.

POSTGRES_USER=
POSTGRES_PASSWORD=
